# === Gemini Live API ===
GOOGLE_API_KEY=your_google_api_key_here
GEMINI_MODEL=gemini-2.0-flash-exp
GEMINI_VOICE=Aoede
SYSTEM_PROMPT=You are a helpful AI assistant in a VR environment.

# === Connection Strategy ===
# ON_DEMAND = connect/disconnect per interaction (lower cost)
# ALWAYS_ON = persistent connection (lower latency)
CONNECTION_MODE=ON_DEMAND

# === Audio Settings ===
INPUT_SAMPLE_RATE=16000
OUTPUT_SAMPLE_RATE=24000

# === WebSocket Server ===
WS_HOST=0.0.0.0
WS_PORT=8765

# === History (On-Demand mode) ===
MAX_HISTORY_MESSAGES=20

# === Debug ===
LOGGING_LEVEL=INFO # Debug or INFO

# Reproduz áudio localmente no backend para testar qualidade
DEBUG_PLAY_AUDIO_LOCALLY=false
LOGGING_LEVEL=INFO